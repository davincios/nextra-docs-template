# Forge with Next.js

```jsx filename="demo.js" {0} copy
import { useFileUpload } from '@forge/upload';

export default function UploadComponent() {
  const { FileInput, uploadFile } = useFileUpload();
  return <FileInput onChange={(file) => uploadFile(file)} />;
}
```

## Preview

import { useFileUpload } from '../components/useFileUpload.tsx';

export default function UploadComponent() {
  const { FileInputComponent } = useFileUpload();
  return <FileInputComponent />;
}

## Component

import { useState } from 'react';
import styles from '../components/counters.module.css';

export const Counter = () => {
  const [count, setCount] = useState(0);
  return (
    <div>
      <button onClick={() => setCount(count + 1)} className={styles.counter}>
        Clicked {count} times
      </button>
    </div>
  );
};

<Counter />

## External Component

import Counters from '../components/counters';

<Counters />
